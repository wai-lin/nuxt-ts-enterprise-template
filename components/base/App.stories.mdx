import { Meta, Canvas, Story, Props } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'

import BaseApp from './App.vue'

<Meta title="BaseApp" component={BaseApp} />

# Example

[WIP]

Add the `BaseApp` component in every layout as base component.
This will provide Snackbar (Alert), Theme, Global Fetching etc.

# Props

<Props of={BaseApp} />
